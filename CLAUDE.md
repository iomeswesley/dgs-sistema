# Contexto do projeto — sistema-dgs

Leia isto no início de qualquer sessão nova. O desenho completo do produto está no [PLANO.md](PLANO.md) e os textos de WhatsApp em [TEMPLATES-WHATSAPP.md](TEMPLATES-WHATSAPP.md) — aqui ficam o estado atual e as convenções operacionais.

## O que é

Ferramenta **interna** da DGS (D'Artibale Gestão em Saúde), empresa que intermedia secretarias municipais de saúde e médicos contratados em SC. Recebe listas diárias de agendamento (PDF nativo gerado pelo SISREG ou CELK — nunca foto), extrai localmente sem IA, dispara confirmação por WhatsApp com botões Sim/Não e concilia o atendimento em três checagens.

Não é multi-tenant, não tem cobrança e não tem perfis de acesso: **perfil único**, todo mundo da equipe pode tudo. O controle vem da tabela `audit_logs`, não de permissões — decisão explícita do usuário.

- Repo: [github.com/iomeswesley/dgs-sistema](https://github.com/iomeswesley/dgs-sistema) (privado), branch `master`.
- Deploy: **publicado em produção desde 2026-08-14/15** — `https://sistema-dgs.vercel.app` (projeto Vercel `innova-ia/sistema-dgs`). `vercel.json` + `api/index.js`.
- Banco de **produção**: Postgres/Supabase (`aws-0-sa-east-1.pooler.supabase.com`), projeto `koplspjaqazgsvcaspmp`. **Em uso de verdade agora** — deixou de ser só referência em `.env.production`. O projeto Supabase é free tier e **pausa sozinho por inatividade** (aconteceu em 2026-08-14: precisou reativar manualmente no dashboard antes de `prisma migrate deploy` funcionar de novo — se der erro `tenant/user ... not found`, é isso, não é bug).
- Banco de **dev local**: Postgres 17 em container Docker (`docker-compose.yml` na raiz, serviço `postgres`, container `dgs-postgres`), banco `sistema_dgs`, usuário `postgres`, senha `dgs_local_dev`, porta 5432.
- **⚠️ O `.env` ativo desta máquina está apontando pro banco de PRODUÇÃO desde o deploy de 2026-08-14/15**, não mais pro Docker local — troca deliberada, mantida assim por decisão do usuário ("sem problemas deixar produção pro Vercel direto"). Qualquer script rodado localmente (seed, reset de senha, `npx prisma ...`) afeta o banco real. Pra voltar a apontar pro Docker local, trocar `DATABASE_URL`/`DIRECT_URL` de volta — só fazer isso se o usuário pedir.

## Estado atual (2026-08-15)

**Fase 1 e Fase 2 do PLANO.md implementadas e em produção.** Primeiro deploy real feito em 2026-08-14. Validado de ponta a ponta contra produção, com dado real:

- Login, navegação, upload/preview/revisão/aprovação/disparo — tudo testado contra o banco e o WhatsApp de produção.
- **WhatsApp 100% validado com pessoas reais**: envio → entrega → clique no botão → webhook → classificação da resposta, tudo confirmado (ver "WhatsApp em produção" abaixo).
- **Extração testada com 3 PDFs reais de 2 prefeituras** (Camboriú/CELK ×2, Pomerode/SISREG ×1) — os três reconhecidos e extraídos com sucesso (23/23, 30/30, 50/50 pacientes). Só essas 2 prefeituras têm calibração confirmada; qualquer prefeitura nova precisa de um PDF de exemplo pra confirmar antes (`npm run extrair -- caminho/do/arquivo.pdf`) — ver pendência sobre isso mais abaixo.
- **Disparo em massa real testado de ponta a ponta** (Lista → Revisão → Aprovar → Disparar, não só o atalho "Enviar teste") com 3 números reais de pessoas da equipe — confirmações chegaram, respostas de botão (Sim/Não) voltaram certinho pelo webhook.

**Escopo reduzido por decisão do usuário em 2026-08-09**: o módulo financeiro (repasse ao médico, faturamento, margem — `doctor_fee`/`city_rate`) não entra nesta fase do projeto. O produto agora é só agendamento + confirmação de comparecimento via WhatsApp; ver PLANO.md.

**Cadastro de produção está vazio de propósito** (checado e limpo em 2026-08-15 — havia um resquício de teste de 2026-07-27 esquecido lá, removido). Precisa popular com município/unidade/médico/procedimento reais antes do cliente operar pra valer.

**✅ WhatsApp em produção voltou a ter credencial ativa em 2026-08-18**, via botão "Conectar WhatsApp (número novo/limpo)" — testado de ponta a ponta pela primeira vez com sucesso: popup completo, evento `FINISH` recebido (`wabaId: 2046188346288778`, `phoneNumberId: 1253723657826772`), `POST /api/whatsapp/signup/callback` salvou a conta, e ela aparece em Configurações → WhatsApp. Isso resolve o período de "nenhuma mensagem sai" que durou desde 2026-08-16. Ver "WhatsApp em produção" mais abaixo pro histórico completo da investigação (CSP/COOP, evento de coexistência, app "Unpublished", campos de webhook, `featureType`).

**Conectar um número novo não é o fim da história — faltavam mais dois passos, agora automatizados (2026-08-18)**: o "Enviar teste" nessa WABA nova falhou com `(#133010) Account not registered` (o número precisa ser registrado na Cloud API, passo separado do Embedded Signup) e, depois de resolver isso, com `(#132001) Template name does not exist` (WABA nova nasce sem nenhum template aprovado — templates são por WABA, não compartilhados). **`registerPhoneNumber()` e `submitDefaultTemplates()`** (`src/lib/whatsapp-templates.ts`) agora rodam sozinhos dentro de `POST /api/whatsapp/signup/callback` toda vez que uma conta conecta — best-effort, não desfazem a conexão se falharem. Configurações → WhatsApp mostra um aviso "⏳ Templates aguardando aprovação da Meta" com status por template enquanto não aprovar, e outro aviso separado se o erro 131042 (sem forma de pagamento) aparecer no envio mais recente, com botão direto pro billing hub. Ainda falta a Meta aprovar de fato os 3 templates da WABA `2046188346288778` (submetidos manualmente via script nesse mesmo dia, já que a automação só roda em conexões novas — essa WABA já existia antes do código existir).

**Coexistência (número que já usa WhatsApp Business App) continua sem confirmação de sucesso** — mesmo depois de todos os fixes abaixo (webhook, `featureType`, reset da WABA travada), o popup ainda trava em "981536728049005 isn't a valid Business ID" ao tentar confirmar o compartilhamento com o app. Hipótese em teste: falta a permissão `business_management` estar com Advanced Access publicada (estava "Not submitted" — App Review submetido em 2026-08-18, aguardando aprovação da Meta). Enquanto isso não aprova, coexistência com número de cliente externo fica bloqueada; número novo/limpo funciona normalmente.

### Pendências abertas
1. **Coexistência do WhatsApp ainda travada** — App Review de `business_management` submetido (2026-08-18), aguardando aprovação da Meta. Se aprovar e ainda travar, o próximo passo já é abrir chamado de suporte com a Meta (erro não documentado publicamente, código de referência `#1690130`).
2. Popular o cadastro de produção antes do cliente operar pra valer.

## Sessão de 2026-08-26 — resumo (detalhe completo nas 18 entradas "Resolvido em 2026-08-26" abaixo)

Cliente testando o sistema de ponta a ponta pela primeira vez (Listas, Revisão, Cancelamento, Conversas), vários bugs e pedidos de UX reais surgiram e foram resolvidos no mesmo dia:

- **Confiabilidade do disparo**: `processQueue()` ganhou `TIME_BUDGET_MS` (pára sozinho antes do timeout de 60s da Vercel) + `web/src/lib/queue.ts` (`runQueueUntilDone`, o frontend completa o envio sozinho em requisições separadas) — nunca mais trava nem depende do cron do dia seguinte. Usado em Cancelamento, dispatch de Lista, Acompanhamento e "Adicionar paciente manualmente".
- **Cancelamento** ganhou: "Respondeu" (qualquer resposta, não só template), coluna de telefone + filtro por situação da mensagem + legenda, reenvio pra quem falhou/sem telefone (com telefone alternativo do cadastro), reconciliação "extraído × restante", cores por status, coluna "Situação" unificada (resposta conta como "Lido").
- **Revisão** ganhou a mesma dinâmica: filtro por situação, reenvio pra quem falhou/sem telefone, legenda, e o **botão "Adicionar paciente manualmente"** (`addManualAppointment()`, `POST /api/lists/:id/appointments`) pra "Registro não reconhecido" — funciona em qualquer status da lista (não só em revisão), manda a mensagem na hora se a lista já foi disparada.
- **Conversas** ganhou busca por nome/telefone; corrigido bug sério de `phoneCandidates()` só reconstruir telefone num sentido (12→13 dígitos, não o inverso) — causava mensagem sumindo do chat, janela de 24h errada, e "Respondeu" do Cancelamento quase todo errado.
- **Parser SISREG, dois bugs sérios achados com PDF real**: registro sem horário sumia da lista inteira (hora virou opcional), e registro que atravessa quebra de página do PDF (2+ telefones) tinha risco real de vazar CID-10 (dado sensível) pro resultado — corrigido com corte global em vez de ancorado no fim do texto, e filtro do rodapé de página que sujava o meio do registro.
- **Outros bugs de dado corrigidos**: nome duplicado do SISREG (`dedupeRepeatedName`), identidade de paciente (mesma proteção nome+CNS/telefone agora também no caminho por CNS de `resolvePatient`), "duplicado" preso após excluir a outra ocorrência, "Registrar contato" bloqueado em agendamento cancelado.
- **Auditoria de produção**: achado e excluído 1 paciente órfão (telefones comprovadamente de outra pessoa, resíduo do bug de identidade já corrigido em 25/08).
- Confirmado e documentado: templates da WABA ativa aprovados, billing configurado — as duas últimas pendências de WhatsApp fecharam.

### Resolvido em 2026-08-26 (8)
- **Templates da WABA ativa aprovados + billing configurado — as duas últimas pendências da lista de WhatsApp fechadas.** Conferido direto na Meta: os 4 templates (`confirmacao_consulta`, `lembrete_vespera`, `convite_vaga_aberta`, `cancelamento_consulta`) estão `APPROVED`. Billing também: o usuário conferiu o billing hub da Meta e a WABA ativa (`2046188346288778` — aparece com o nome **"Mkt"** no Business Manager, diferente do nome do WABA em si; o nome que o paciente vê no WhatsApp é outro campo, "DGS Agendamentos", do perfil do número) tem cartão configurado (MasterCard ••••9224). Achado no caminho: o Business Manager tem 5 assets — "Mkt" (a WABA ativa de verdade), "innova agendamentos" (a WABA antiga do número aposentado em 2026-08-16, ainda existe, R$0,11 de cobrança histórica), "innova" e "Test WhatsApp Business Account" (não usados pelo sistema-dgs) e "DGS" (asset separado, não é WABA, sem forma de pagamento — não investigado a fundo, não bloqueia nada).

### Resolvido em 2026-08-25
- **Bug sério: horário do agendamento saía 3h a menos** — `new Date("2026-08-27T07:00")` (string sem timezone, vinda da extração de PDF ou de edição manual em Revisão) foi salvo em produção como `07:00 UTC` em vez de `10:00 UTC`, mesmo com `process.env.TZ = "America/Sao_Paulo"` fixado em `server.ts`/`api/index.js` — **`process.env.TZ` setado em runtime não é confiável o bastante pro parsing de string "local" do V8 no ambiente da Vercel**. Resultado: paciente veria (e o WhatsApp mandaria) o horário errado, 3h adiantado. Achado a tempo — só uma lista de teste (2026-08-15, números da equipe) tinha sido disparada de verdade; nenhum paciente real foi afetado. Corrigido na raiz: `parseBrasiliaDateTime()` novo em `lib/timezone.ts` (anexa `-03:00` explícito na string antes de virar `Date`, elimina a ambiguidade sem depender de config de ambiente nenhuma) usado em `lists.service.ts` (extração e edição manual); todo `toLocaleDateString`/`toLocaleTimeString`/`toLocaleString` que também não tinha `timeZone` explícito (mensagem de WhatsApp em `queue.service.ts`, exports CSV em `agendas.routes.ts`/`list-report.ts`, `signup.routes.ts` teste, e o frontend inteiro — `lib/format.ts`, `Configuracoes.tsx`, `Conversas.tsx`, o campo de edição de horário em `Revisao.tsx`) ganhou `timeZone: "America/Sao_Paulo"` também, pra não depender do fuso do navegador de quem está operando. A lista 11 (em revisão no momento do achado) foi reprocessada do zero (`extractAndStage`) já com o fix, confirmado certo.
- **Aviso de "telefone inválido"/"sem data" etc. ficava preso na Revisão mesmo depois de corrigido** — `rawLine.issues` é um retrato de quando a extração rodou, nunca era atualizado quando alguém corrigia o campo na revisão. `clearResolvedIssues()` (`lists.service.ts`, chamado por `editAppointment`) agora tira da lista de avisos o que cada edição resolveu (telefone corrigido tira `telefone_invalido`/`sem_telefone` e limpa `invalidPhones`; data corrigida tira `sem_data`; médico/procedimento corrigidos tiram os avisos correspondentes).
- **Celular de 8 dígitos sem o nono agora é reconstruído sozinho** (`lib/phone.ts`) — decisão antiga era rejeitar ("não dá pra reconstruir com segurança"), mas 8 dígitos começando 6-9 só existe como celular sem o 9 (fixo nunca começa nessa faixa) — é justamente o motivo mais comum de "telefone inválido" nas listas da prefeitura (formato antigo, antes da expansão do 9º dígito). Reconstrução é segura e agora automática.
- **Botão "Filtrar erros" na Revisão** — ao lado da busca por nome/telefone, filtra a tabela pra mostrar só quem ainda tem algum aviso pendente (`rawLine.issues`).
- **Aviso de unidade/endereço divergente**: "Cadastro diz:" ganhou o rótulo **"(vai pra mensagem)"** bem ao lado, pra não depender de ler o parágrafo explicativo mais abaixo pra entender qual dos dois endereços é o que sai de verdade pro paciente.
- **Bug sério: paciente casado com nome errado por coincidência de telefone** — `resolvePatient()` (`lists.service.ts`) casava paciente existente **só pelo telefone**, sem checar o nome, contra qualquer paciente do banco inteiro (não só da lista sendo processada). Achado real na lista 11: duas linhas mostravam "ANDRESA AQUINO ALMEIDA" (paciente 149) quando o PDF não tinha esse nome em lugar nenhum — o paciente 149 já tinha telefones de **4 DDDs diferentes acumulados** (75-BA, 61-DF, 83-PB, 47-SC), prova de que o problema já vinha de antes, não é só de hoje. Corrigido: além do telefone bater, agora também exige `namesMatch()` (mesmo comparador fuzzy de unidade/médico, reaproveitado) entre o nome já cadastrado e o nome dessa linha — telefone batendo com nome bem diferente vira paciente novo, não reaproveita o cadastro errado (número de celular é reatribuído pela operadora com frequência, e fixo pode ser de outra pessoa da casa). Lista 11 reprocessada, confirmado que os dois agendamentos passaram a mostrar os nomes certos (EYSHILLAH DHASMYNIE HENRIQUES DA SILVA LIMA e JACQUELINE AMARAL LOMBARDI). **Pendência que sobra**: o paciente 149 continua com os telefones de outros DDDs acumulados de antes — não foram removidos (não dá pra saber com certeza quais são legítimos sem investigar caso a caso), só o vínculo de agendamento errado foi desfeito. Pode haver outros pacientes com o mesmo tipo de poluição histórica; não foi feita uma auditoria completa da base.

### Resolvido em 2026-08-26 (18)
- **Bug sério no parser SISREG: registro que atravessa quebra de página do PDF podia vazar CID-10 e sempre perdia dado** — achado pelo usuário direto no PDF real (lista 12): quando a linha da tabela de um paciente tem 2+ telefones ou nome longo, o SISREG empurra o resto da linha pra página seguinte, com um bloco de rodapé/cabeçalho de página (`data, hora  SISREG III - Servidor de Producao` + URL + `-- N of M --`) no meio. Duas consequências:
  1. O corte do CID-10 (`remainder.replace(/\s+[A-Z]\d{2,4}\s*$/, "")`, ancorado no **fim** do texto) parava de funcionar assim que sobrava qualquer texto depois do CID-10 — risco real de o código do diagnóstico vazar pro `requestingUnit` e por consequência pro resultado (dado sensível, LGPD). Corrigido: corte agora é global (`\b[A-Z]\d{2,4}\b`, em qualquer posição), não só ancorado no fim — mais seguro mesmo que eventualmente corte algo que só parece CID-10 por coincidência.
  2. O bloco de rodapé/página virava lixo no meio do registro. Corrigido: `PAGE_BREAK_NOISE` filtra essas linhas (e linhas vazias) antes de agrupar em registros.
  - Verificado contra o PDF real da lista 12: os dois pacientes que apareciam como "Registro não reconhecido" (perdidos da lista inteira) agora são lidos certinho, incluindo os telefones extras que ficavam na página seguinte (MIRCEA ganhou o 2º telefone, RONALDO ganhou o 2º e 3º). Único resíduo conhecido: o sobrenome "SILVA" da MIRCEA continua sem ser capturado automaticamente (fica fora da ordem esperada dos campos depois da quebra de página) — precisa de correção manual pontual, caso raro. Teste de regressão novo reproduzindo o cenário real, checando explicitamente que o CID-10 nunca aparece em lugar nenhum do resultado serializado.
  - Lista 12 **não foi reprocessada** (decisão do usuário) — reprocessar recuperaria os 2 pacientes automaticamente, mas apagaria a revisão manual já feita nos outros 108 hoje. Os 2 pacientes foram orientados a entrar via "Adicionar paciente manualmente" com os dados corretos.

### Resolvido em 2026-08-26 (17)
- **Bug sério no parser SISREG: registro sem horário sumia da lista inteiro** — achado pelo usuário (2 pacientes reais da lista 12, "Registro não reconhecido"). Causa: `headMatch` em `sisreg.ts` exigia `\d{2}:\d{2}` (hora) logo depois da data, sempre — mas confirmado no PDF real que às vezes o SISREG não atribui horário nenhum pro paciente (data sim, hora não), e nesse caso o regex simplesmente não casava, rejeitando o registro inteiro sem deixar rastro (só um aviso genérico de "não reconhecido"). Corrigido: hora virou opcional no regex; sem ela, `scheduledAt` sai `null` e a linha entra na revisão marcada `sem_data` (mesmo tratamento que qualquer outro campo ausente já recebe) — a equipe completa a data manualmente, mas o paciente não desaparece mais. Teste novo com o caso real.
- **Botão "+ Adicionar paciente manualmente" na revisão** — pedido do usuário: até aqui não existia jeito nenhum de completar a lista com quem a extração não conseguiu reconhecer, só reprocessar do zero (o que perderia qualquer correção manual já feita). Aparece dentro da própria caixa de avisos, abre formulário (nome, CNS opcional, telefone, data/hora — pré-preenchida com médico/procedimento que o resto da lista já usa —, médico, procedimento, primeira vez). `addManualAppointment()` (`lists.service.ts`, rota `POST /api/lists/:id/appointments`) reaproveita `resolvePatient()` — mesma proteção de identidade (CNS/nome/telefone) da extração normal, não cria paciente duplicado se a pessoa já está cadastrada.
  - **Corrigido no mesmo dia**: nasceu restrito a `EM_REVISAO`, igual editar/remover — mas o usuário notou que o botão sumia numa lista já disparada, exatamente quando dá mais falta (normalmente só se percebe quem ficou de fora DEPOIS de disparar, quando a edição normal já está trancada). Agora funciona em qualquer status; se a lista já passou da revisão, a mensagem sai pra essa pessoa na hora (enfileira + `processQueue()`, mesma convenção do dispatch normal, frontend completa com `runQueueUntilDone` se não couber numa chamada só) — não fica esperando um "disparar" que não existe mais pra ela.
- **Caixa de avisos da leitura virou vermelha, com ⚠️** — antes era amarela (`tone="warn"`), igual o resto dos avisos de menor severidade; "Registro não reconhecido" é perda de dado de verdade (o paciente nem virou linha na revisão), merece se destacar mais. `Callout` ganhou `tone="danger"` (vermelho + ícone), reaproveitável em qualquer outro aviso crítico do sistema.
- **Faixa "Precisa de ação" agora soma quem nem virou agendamento** — pedido do usuário: os "Registro não reconhecido" da leitura não entravam em nenhuma contagem (não são `Appointment`), então o resumo no topo escondia esses casos. `StatusBand` ganhou `unrecognizedCount` opcional — mostra "25 + 2" (o "+2" em vermelho) só na legenda da faixa "Precisa de ação", sem mexer na proporção da barra (que reflete só agendamentos de verdade).

### Resolvido em 2026-08-26 (16)
- **Bug: "Registrar contato" em Acompanhamento sobrescrevia CANCELADO em silêncio** — achado pelo usuário: o botão aparecia mesmo pra agendamento com status `CANCELADO` (agenda cancelada pela equipe, não tem presença pra confirmar). Se alguém clicasse e registrasse Confirmou/Recusou, `POST /api/appointments/:id/contact` sobrescrevia o status sem aviso nenhum, perdendo o fato de que a agenda foi cancelada. Corrigido nos dois lados: `Hoje.tsx` esconde o botão quando `status === "CANCELADO"` (mostra só "—" com tooltip explicando), e a rota agora bloqueia com 409 mesmo se alguém chamar a API direto.
- **Campo de busca por nome/telefone em Conversas** — mesmo padrão já usado em Revisão, filtra a lista de conversas na hora.

### Resolvido em 2026-08-26 (15)
- **Bug: aviso "duplicado" ficava preso pra sempre mesmo depois de excluir a outra ocorrência** — achado pelo usuário (ADILSON LOOS, lista de 31/08, aparecia sozinho na tabela mas ainda destacado em amarelo com "duplicado"). `removeAppointment()` (`lists.service.ts`) só apagava a linha, nunca atualizava `rawLine.issues` de quem sobrou — `rawLine` é um retrato de quando a extração rodou (mesma classe de bug já corrigida em `clearResolvedIssues`, mas essa dispara ao editar, não ao remover). Corrigido: depois de excluir, verifica se sobrou só 1 agendamento com aquele `patientId` na lista e, se sim, tira "duplicado" do que ficou.
  - **Varredura em produção**: achado mais 9 casos presos do mesmo jeito, em 2 listas diferentes (11 e 12) — todos corrigidos direto no banco.

### Resolvido em 2026-08-26 (14)
- **Auditoria dos telefones poluídos, pedida pelo usuário** — dois níveis: (1) pacientes com telefones de DDDs diferentes (24 achados, maioria explicável — gente que migrou de estado ou tem parente longe, não é bug); (2) muito mais preciso, **telefone que aparece em mais de um cadastro de paciente diferente** (celular é pessoal, não se divide — sinal forte de bug). Achou 5 números compartilhados; a maioria são linhas fixas plausivelmente de família (várias pessoas da mesma casa cadastradas separadamente, normal). Um caso confirmado como resíduo do bug antigo: **paciente 149 (Andresa Aquino Almeida)** tinha 2 celulares que provadamente pertenciam ao paciente 263 (Eyshillah, criada corretamente depois do fix de 25/08) — conferido contra a linha original do PDF da Eyshillah, os números batem certinho com ela. "Andresa" não aparece em lugar nenhum da lista de origem, e o registro não tinha nenhum agendamento vinculado — órfão, puro resíduo do merge indevido de antes do fix. **Excluído do banco de produção com confirmação do usuário.**
- **Segundo lugar no código com a mesma falha de identidade, achado e corrigido** — o casamento por CNS em `resolvePatient()` (`lists.service.ts`) também juntava telefones sem checar se o nome batia (só o caminho por telefone tinha sido corrigido em 25/08). CNS colide muito menos que telefone (por isso não tinha se manifestado visivelmente ainda), mas o mesmo risco existe se a extração colar um CNS certo num nome errado (já visto antes no parser SISREG, por outro motivo). Corrigido com `namesMatch()` também nesse caminho — e, como CNS é `@unique` no banco, cuidado extra pra não tentar salvar um CNS que já pertence a outro paciente quando o nome não bate (levaria a um erro de unicidade): nesse caso o paciente novo nasce sem CNS nenhum, em vez de travar ou reaproveitar errado.

### Resolvido em 2026-08-26 (13)
- **Colunas "Respondeu" e "Mensagem" unificadas no Cancelamento** — pedido do usuário: às vezes o status bruto mostrava "Entregue" mas o paciente já tinha respondido, parecendo contraditório entre as duas colunas. `effectiveMessageStatus()` (`CancelamentoDetalhe.tsx`) promove Enviado/Entregue pra "Lido" quando `replied` é `true` — resposta é prova mais forte de que o paciente viu do que o recibo de leitura opcional da Meta. As duas colunas viraram uma só ("Situação"), com o texto da resposta aparecendo embaixo do badge quando existe. Filtro e contagem por status também usam o valor "efetivo", não o bruto.

### Resolvido em 2026-08-26 (12)
- **Badges de situação da mensagem no Cancelamento estavam todos cinza** — `MessageStatusBadge` (`CancelamentoDetalhe.tsx`) só diferenciava Falhou (vermelho) de todo o resto (cinza). Corrigido: Enviado/Entregue em amarelo (em trânsito, mesmo tom que o resto do sistema usa pra "aguardando"), Lido em verde (desfecho positivo), Falhou continua vermelho.

### Resolvido em 2026-08-26 (11)
- **Faixa "Sem telefone" do `StatusBand` renomeada pra "Precisa de ação"** — achado pelo usuário: essa faixa nunca foi só sem telefone, sempre somou `SEM_TELEFONE + FALHA + SEM_RESPOSTA` (`toBandCounts()`, `web/src/lib/format.ts`) — uma lista mostrou 25 nela sendo 8 sem telefone + 17 falha, e o botão de reenvio (que também soma esses dois) batia o mesmo número por coincidência (`SEM_RESPOSTA` era 0 ali), o que escondia a confusão. Esclarecido com o usuário: `SEM_RESPOSTA` não é o mesmo que "Aguardando" — é quando o prazo estourou e o sistema **desistiu** de esperar resposta (fechado automaticamente pela cadência), diferente de "Aguardando" (`PENDENTE+ENVIADO+ENTREGUE`), que ainda pode virar confirmação sozinho. As três situações da faixa renomeada têm em comum que **nenhuma resolve sozinha** — precisa de alguém completar telefone, reenviar ou ligar. Nome novo mais fiel a isso; composição exata (`"Sem telefone + falhou + estourou prazo sem resposta"`) fica no `title` (tooltip ao passar o mouse), pra não estourar o espaço nos cards compactos de Listas. Usado em Listas, Acompanhamento e Revisão.

### Resolvido em 2026-08-26 (10)
- **Bug sério: paciente "sem telefone" ficava num limbo sem jeito de corrigir, tanto em Listas quanto em Cancelamento** — achado pelo usuário. Em Listas: depois do disparo (`DISPARADA`), a edição normal (`editAppointment`/"Corrigir") só funciona com a lista em `EM_REVISAO` — quem nasceu `SEM_TELEFONE` ficava travado nesse status pra sempre, sem telefone nenhum pra completar. Corrigido: "Reenviar pra quem falhou" (Revisão e Cancelamento) passou a incluir `SEM_TELEFONE` junto com `FALHA`/sem envio — mesmo formulário, telefone em branco pra equipe preencher.
  - Em Cancelamento era pior: `eligibleAppointments()` excluía `SEM_TELEFONE` da notificação (correto, não tem pra onde mandar), mas **o agendamento nunca era tocado** — ficava com `status: SEM_TELEFONE` (nunca virava `CANCELADO`) e nunca era linkado a nenhum `CancellationBatch`, então não aparecia em tela nenhuma pra sempre, mesmo a agenda dele tendo sido cancelada de verdade. Corrigido: `dispatchCancellation()` agora também marca esses como `CANCELADO` e linka ao lote (sem criar `MessageJob`, não tem telefone) — aparecem no detalhe do lote na aba "Sem envio", e entram no "Reenviar pra quem falhou/sem telefone". `previewCancellation()` também passou a listar esses casos antes de disparar, com aviso claro de que serão cancelados mas ninguém é avisado.
  - Conferido em produção: o único lote de cancelamento existente (EDIVALDO ANTONIO MACHADO) não tinha nenhum paciente sem telefone — sem pendência retroativa a corrigir nos dados.
- **Legenda "O que significa cada situação" também em Revisão** — mesmo padrão de caixinha que já existia no Cancelamento, mostrando só os status que realmente aparecem naquela lista (Pendente/Enviado/Entregue/Confirmado/Recusado/Sem resposta/Sem telefone/Falha/Cancelado).

### Resolvido em 2026-08-26 (9)
- **Mesma dinâmica do Cancelamento trazida pra Revisão (lista normal de confirmação)** — pedido do usuário depois de o cliente testar e ver "Falha no envio" em algumas linhas: botões de filtro por situação (Pendente/Enviado/Entregue/Confirmado/Recusado/Sem resposta/Sem telefone/Falha, só mostra os que têm pelo menos 1) e botão "Reenviar pra quem falhou" — mesmo formulário com telefone por paciente, pré-preenchido com outro celular do cadastro quando existe.
  - `pickAlternatePhone()` (achava só em `cancellations.service.ts`) virou helper compartilhado em `lib/phone.ts`, usado pelos dois.
  - `retryFailedAppointments()` (novo, `lists.service.ts`, rota `POST /api/lists/:id/retry-failed`) — diferente do Cancelamento (que sempre usa o template CANCELAMENTO), aqui o template certo depende da lista: `CONFIRMACAO` normal ou `VAGA_ABERTA` se `list.isComplementary` (mesma regra que `enqueueList()` já usava). Não exige a lista estar em `EM_REVISAO` — o objetivo é corrigir DEPOIS do disparo, quando a edição normal (`editAppointment`) já não é mais permitida.
  - `GET /api/lists/:id` ganhou `alternatePhone` por agendamento na resposta.
  - Verificado contra produção (lista 12, real, do cliente testando): `pickAlternatePhone()` achou número alternativo pra maioria dos 17 que falharam, `null` só pra quem realmente só tem o número que já foi tentado.
- **Confirmado, não é bug**: as falhas de CONFIRMACAO batem exatamente com as mesmas causas do Cancelamento — quase sempre código `131026` "Message undeliverable" da Meta (sem WhatsApp/inválido/inalcançável, sem distinguir qual). Achado interessante: os 17 números que falharam na CONFIRMACAO original da lista 11 são os MESMOS 17 que falharam de novo no cancelamento dessa mesma agenda — faz sentido, é o mesmo número de telefone tentado duas vezes.
- **Achado no caminho, não investigado a fundo**: um paciente da lista 12 apareceu como "SILVIA KUSTER CORREA NARCISO SILVIA NARCISO" — parecido com o bug de nome duplicado do SISREG já corrigido (`dedupeRepeatedName()`), mas o padrão é diferente (repetição parcial, não a string inteira igual duas vezes) — o dedupe atual não pega esse caso de propósito (evita mexer em nome que pode ser legítimo, tipo nome social genuinamente diferente do civil). Não confirmado se é bug ou dado real; fica como possível pendência pra revisitar se aparecer de novo.

### Resolvido em 2026-08-26 (7)
- **Reconciliação "extraído do PDF × restou depois da revisão" no Cancelamento** — pedido do usuário depois de perguntar por que uma lista de 113 pacientes virou 109 no cancelamento (resposta: 4 linhas removidas na revisão por duplicata/paciente errado, um recurso que já existia — `removeAppointment()`/`remove_review`, confirmado via audit log). Pra quem for conferir o número não precisar perguntar de novo nem ir no log: `getExtractionReconciliation()` (`cancellations.service.ts`) compara `List.extractionRaw.rows.length` (quantos a extração leu do PDF) com a contagem atual de `Appointment` daquela lista, e `/cancelamentos/:id` mostra a frase pronta ("X extraídos, Y removido(s) na revisão, Z restaram") dentro do card do motivo. `null` quando a origem não tem uma `List` com extração associada (agenda criada à mão). Verificado contra produção: lote 1 mostra `113 extraídos, 109 restantes`, batendo exatamente com o achado.

### Resolvido em 2026-08-26 (6)
- **Reenvio pra quem falhou no Cancelamento** — botão "Reenviar pra quem falhou" (aparece só quando há algum `FALHOU`) abre um formulário, um campo de telefone por paciente, pré-preenchido com **outro celular do cadastro** quando existe (`alternatePhone`, calculado em `getCancellationBatch()` a partir de `Patient.phones`, excluindo o que já foi tentado) — em branco quando não tem outro, pra equipe digitar. Campo vazio = não reenvia pra esse paciente. `retryFailedMessages()` (`cancellations.service.ts`, rota `POST /api/cancellations/:id/retry-failed`) atualiza `Appointment.selectedPhone` pro número novo e enfileira um `MessageJob` CANCELAMENTO novo — não mexe em `status` (continua `CANCELADO`). Reenvio usa o mesmo `runQueueUntilDone` do frontend (não trava nem depende do cron, ver achado anterior do dia).
- **Legenda da situação da mensagem** — caixinha acima da tabela em `/cancelamentos/:id` explicando Enviado/Entregue/Lido/Falhou. Motivada pela pergunta do usuário "como que tem caso 'Entregue' com resposta?" — resposta: recibo de leitura ("Lido") é opcional no WhatsApp do paciente, muita gente desativa; a mensagem chega e pode ser respondida normalmente mesmo sem nunca virar "Lido" pro remetente.
- **Confirmado, não é bug**: dos 18 `FALHOU` reais em produção até aqui, 17 são o código Meta `131026` "Message undeliverable" (número sem WhatsApp/inválido/inalcançável — a Meta não distingue qual dos três) e 1 é `131042` (forma de pagamento, já resolvido). Não é "só quando não acha o número" — é o balde genérico de "não deu pra entregar".
- **Confirmado, não é bug**: o teto de 250 mensagens/dia não é fixo no código — vem ao vivo da Meta (`messaging_limit_tier` do número, `whatsapp-account.service.ts`), que sobe sozinha conforme o histórico de qualidade (250 → 1K → 10K → 100K → ilimitado). Conferido em produção: `qualityRating: GREEN`, mas `messagingLimitTier` ainda `null` (número novo, conectado 2026-08-18) — cai no padrão do `.env`. Não tem como aumentar via código; só o próprio histórico de uso sem denúncia/bloqueio faz a Meta subir o tier.

### Resolvido em 2026-08-26 (5)
- **Detalhe do Cancelamento (`/cancelamentos/:id`) ganhou coluna de Telefone e filtro por situação da mensagem** — pedido do usuário: coluna nova mostra o número pro qual a mensagem foi mandada (`CancellationBatchDetail.appointments[].phone`, backend), e botões acima da tabela filtram por Enviado/Entregue/Lido/Falhou/Sem envio (com contagem de cada um), igual ao padrão "Filtrar erros" já usado em Revisão.

### Resolvido em 2026-08-26 (4)
- **Bug sério: resposta do paciente sumia do chat principal em Conversas (aparecia só na lista da esquerda), e o aviso "fora da janela de 24h" aparecia errado mesmo com resposta recente** — achado pelo usuário em dois lugares (o chat de Conversas e o "Respondeu" novo do Cancelamento, que só mostrava 1 de ~46 respostas reais no lote do EDIVALDO ANTONIO MACHADO). Causa: `phoneCandidates()` (`lib/phone.ts`) só reconstruía num sentido — de 12 dígitos (sem o 9º dígito, formato que a Meta às vezes manda no `from` do webhook) pra 13 (com o 9). `getThread()`, `sendReply()` (checagem da janela de 24h) e `getCancellationBatch()` (o "Respondeu" novo) todos partem do telefone já normalizado (13 dígitos, com o 9) e usam `phoneCandidates()` pra montar o `WHERE phone IN (...)` — como a função nunca gerava o candidato de volta pra 12 dígitos, a busca simplesmente não batia contra a linha `RECEBIDA` gravada com o `from` cru da Meta. A lista de conversas da esquerda não tinha esse problema porque agrupa por outro caminho (`normalizePhone` de cada mensagem individualmente, que já reconstruía os dois lados certo) — por isso só um sumia, não o outro, o que tornou o bug mais difícil de enxergar.
  - Corrigido tornando `phoneCandidates()` bidirecional: de 13 dígitos (com o 9) também gera o candidato de 12 (sem o 9). Testes novos em `phone.test.ts`.
  - Verificado contra produção depois do fix: o lote do EDIVALDO ANTONIO MACHADO foi de 1 pra 46 respostas corretamente detectadas (de 109 pacientes).

### Resolvido em 2026-08-26 (3)
- **Bug: nome do paciente saía duplicado ("FULANA DE TAL FULANA DE TAL")** — achado pelo usuário (`JORDANA MARIA AJALA DESSBESELL JORDANA MARIA AJALA DESSBESELL`), confirmado que eram 5 pacientes no banco, 4 da mesma lista 11 (SISREG). Causa: o campo "Nome Social" do SISREG normalmente vem "---" quando o paciente não tem nome social distinto, mas em alguns registros vem com o nome civil repetido em vez de "---" — e o parser (`sisreg.ts`) junta "Nome" + "Nome Social" numa string só antes de achar a data de nascimento, então o nome duplicado ia direto pro cadastro. Corrigido com `dedupeRepeatedName()`: colapsa quando a segunda metade das palavras do nome é idêntica, palavra por palavra, à primeira — não afeta nenhum nome real (ninguém se chama pela repetição exata do próprio nome inteiro). Teste novo em `sisreg.test.ts`. Os 5 pacientes já cadastrados corrigidos direto no banco de produção (só o texto do nome, nada mais do cadastro).
- **Novo: "Respondeu" na tela de detalhe do Cancelamento** — pedido do usuário: em `/cancelamentos/:id`, cada paciente agora mostra em verde se respondeu **qualquer coisa** depois do aviso (não só o clique no botão pronto do template "Ciente, obrigado(a)") — texto livre conta igual. `getCancellationBatch()` (`cancellations.service.ts`) casa por telefone (mesmo `phoneCandidates` que Conversas usa, pelo mesmo motivo: o formato bruto salvo varia entre envio e recebimento) contra qualquer `WhatsappMessage` `RECEBIDA` daquele número, mostra a última resposta como preview.

### Resolvido em 2026-08-26 (2)
- **Bug sério: disparo grande (cancelamento de 109 pacientes) travou em "aguarde" e deu "não foi possível completar a ação"** — a rota (`POST /api/cancellations`, mesmo padrão em `POST /api/lists/:id/dispatch`) criava o lote e depois chamava `processQueue()` **inteiro, de forma síncrona**, dentro da mesma requisição HTTP. Com throttle de 250ms entre envios + latência real da Meta, processar dezenas de mensagens se aproxima ou estoura os 60s de `maxDuration` da função na Vercel — a função é morta no meio, a Vercel devolve uma resposta que não é JSON, e o frontend cai no fallback genérico de `lib/api.ts` ("Não foi possível completar a ação."). Confirmado direto no banco de produção: dos 109, 47 mensagens **realmente saíram** antes da função ser morta, 1 ficou presa em `ENVIANDO` (nem chegou a mandar pra Meta) e 61 nunca foram tentadas — só ficariam pendentes até o cron do dia seguinte rodar sozinho, o que a equipe não pode aceitar pra um aviso urgente de cancelamento.
  - Corrigido em duas camadas:
    1. **`processQueue()` (`queue.service.ts`) ganhou `TIME_BUDGET_MS` (45s)** — pára o loop sozinho, de forma limpa, antes de arriscar ser morto pela Vercel no meio de um job. Isso elimina de vez a possibilidade de um `MessageJob` ficar preso em `ENVIANDO` pra sempre (o que exigiu destravar na mão dessa vez). `ProcessResult` ganhou o campo `dueNow` (quantos pendentes já estão no horário de sair agora, distinto de `deferred`, que também conta lembrete/reenvio agendado pro futuro) — é o sinal pro chamador saber se precisa chamar de novo já.
    2. **Novo `web/src/lib/queue.ts` (`runQueueUntilDone`)** — depois de qualquer disparo (Cancelamento, dispatch de Lista em Revisão, botões "Enviar da fila"/"Rodar cadência" em Acompanhamento), o frontend chama `POST /api/queue/process` repetidamente, em requisições HTTP separadas (cada uma sob o limite de tempo da função), até não sobrar mais nada pra agora — mostrando progresso ao vivo ("X enviadas até agora..."). Nunca mais depende do cron do dia seguinte pra terminar um disparo de hoje, nem de alguém ir conferir no banco se realmente foi tudo.
  - `BATCH_SIZE` (60 jobs por chamada) mantido — o `TIME_BUDGET_MS` é quem garante a segurança contra timeout agora, o batch size é só um teto de consulta/memória razoável, não precisa ser exato.
  - **Correção manual feita no banco de produção** (com confirmação do usuário antes): destravado o job preso em `ENVIANDO` (voltou pra `PENDENTE`) e rodado `processQueue()` localmente (fora da Vercel, sem limite de 60s) em loop até esvaziar — as 62 mensagens que faltavam (61 nunca tentadas + a que tinha travado) saíram de verdade. Nenhum paciente ficou sem ser avisado do cancelamento.
- **Bug menor achado no caminho: template CANCELAMENTO não podia ser enviado por Conversas** — `templateSchema` em `conversations.routes.ts` (`POST /api/conversations/:phone/template`) validava só `CONFIRMACAO | LEMBRETE | VAGA_ABERTA`; o frontend já oferecia CANCELAMENTO na lista (usado pra reabrir conversa fora da janela de 24h), mas o backend rejeitava. Corrigido, incluído no enum.
- **Verificado, sem bug**: a suspeita de que respostas de paciente pararam de aparecer em Conversas não se confirmou — conferido direto no banco (`listConversations()` rodado contra produção) que as mensagens mais recentes apareciam corretamente ordenadas, a poucos segundos de distância do momento da checagem. Provavelmente só uma tela desatualizada no navegador (o auto-refresh de Conversas é a cada 15s, ver `REFRESH_MS`) ou cache de um deploy de frontend anterior.

### Resolvido em 2026-08-26
- **Bug sério: agenda aparecia um dia a menos** — achado pelo usuário em duas telas (Cancelamento, ao escolher a agenda na lista suspensa, e o card de lista no menu principal de Listas, "agenda de 26/08" numa lista que era pro dia 27/08). Causa: `Agenda.date` (e `DailyClosing.date`) são colunas `@db.Date` — já guardam meia-noite **UTC** do dia certo, sem hora nenhuma que faça sentido converter — mas estavam sendo formatadas com `toLocaleDateString(..., { timeZone: "America/Sao_Paulo" })`, a mesma função certa pra timestamp de verdade (`Appointment.scheduledAt`). Meia-noite UTC de 27/08 menos 3h de fuso vira 26/08 21h em Brasília — sempre um dia a menos, sempre. Diferente do bug de 2026-08-25 (esse era string sem timezone sendo mal interpretada); esse é o oposto: um valor **já certo** sendo convertido de fuso quando não devia.
  - Corrigido criando uma distinção explícita em todo o código entre as duas semânticas, documentada nos comentários de cada helper: **timestamp de verdade** (`Appointment.scheduledAt`, `List.createdAt`, `WhatsappAccount.connectedAt`, `CancellationBatch.createdAt`) sempre formata com `timeZone: "America/Sao_Paulo"` explícito (`formatDateTime`/`formatDate` no frontend, `toBrasiliaDateString()` novo em `lib/timezone.ts` no backend); **coluna `@db.Date`** (`Agenda.date`, `DailyClosing.date`, `Patient.birthDate`) sempre lê os componentes **UTC** direto, nunca passa por fuso — `formatCalendarDate()` novo em `web/src/lib/format.ts` (frontend) e um `closingDateKey()`/`agendaDateString()` local em cada service que precisa (backend).
  - Frontend: `Listas.tsx`, `Configuracoes.tsx` (agenda), `Fechamento.tsx`, `Cancelamentos.tsx`, `CancelamentoDetalhe.tsx` trocaram `formatDate` por `formatCalendarDate` em todo lugar que mostra `agenda.date`/`closing.date`.
  - Backend: `cancellations.service.ts` (`CancellationSourceInfo.date` passou a ser `string` já resolvido — `agendaDateString()` pro caminho por Agenda, `toBrasiliaDateString()` pro caminho por List, já que ali a origem é `firstAppointment.scheduledAt`, um timestamp de verdade, não um `@db.Date`); `closings.service.ts` (a função `dateKey()` misturava as duas semânticas num só helper — usada tanto pra `appointment.scheduledAt` quanto pra `closing.date` — separada em `toBrasiliaDateString()` pro primeiro e `closingDateKey()` (UTC) pro segundo); `indicators.ts` (o agrupamento "por mês" tinha o mesmo problema, `item.date.getFullYear()`/`getMonth()` misturando `appointment.scheduledAt` e `closing.date` local — separado em `monthKeyFromTimestamp()`/`monthKeyFromCalendarDate()`, cada chamador resolve o mês antes de entrar no núcleo puro).
  - **Varredura completa pedida pelo usuário** ("aproveita e faz uma varredura completa pra ver se não acha mais bugs desse") — confirmado que os únicos 3 campos `@db.Date` do schema são esses; grep em todo `src/`/`web/src/` por `toLocaleDateString`/`toLocaleString`/getters locais de `Date` não achou mais nenhuma instância da mesma classe de bug. `parseDateOnly()` (`lib/http.ts`, usado por toda rota que recebe `YYYY-MM-DD` — Agenda, Fechamento, Indicadores) endurecido de `new Date(ano, mês, dia)` (fuso local do processo) pra `new Date(Date.UTC(ano, mês, dia))` — na prática já dava o resultado certo (offset de Brasília nunca cruza dia em UTC), mas depender do fuso do processo pra gravar um `@db.Date` é exatamente o padrão que já causou bug antes, então endurecido por precaução mesmo sem evidência de erro real aí.
  - `agendas.routes.ts` (export CSV de vagas abertas, `agenda.date.toISOString().slice(0, 10)`) e `list-report.ts`/`queue.service.ts`/`signup.routes.ts` (mensagem de WhatsApp e export de lista, todos sobre `scheduledAt` com `timeZone` explícito) checados e já estavam corretos — não precisaram de mudança.

### Resolvido em 2026-08-15
- **Excluir lista**: `DELETE /api/lists/:id` (`deleteList()` em `lists.service.ts`) apaga lista + agendamentos + fila de mensagens (cascade no schema). Bloqueia com 409 se já DISPARADA/CONCLUIDA — WhatsApp real já foi pro paciente, nesse caso só dá pra remover linha por linha. Botão em cada card de Listas e nas ações da Revisão, com `ConfirmModal`.
- **Município não pré-preenchia no preview do upload**: não era bug de lógica — o `/api/lists/preview` estava caindo por causa do crash de extração em produção (`DOMMatrix`/worker, já corrigidos em `2ff37fa`/`1bb0bc1`), e o frontend engolia o erro em silêncio. Testado com PDF real de produção (Camboriú): município/unidade/agenda batem certinho. `Listas.tsx` agora mostra erro visível quando o preview falha, em vez de falhar calado.
- **Checagem de unidade/endereço antes de aprovar e disparar**: `checkUnit()` (`lists.service.ts`) compara a unidade da agenda vinculada (cadastro) com o texto que a extração leu no PDF, exposta como `unitCheck` em `GET /api/lists/:id`. `approveList()` **bloqueia no backend** (409) se houver problema (sem agenda, agenda sem unidade, unidade sem endereço, ou PDF×cadastro não batendo) e a equipe não tiver marcado `confirmUnitMismatch`. Revisão mostra o comparativo "PDF diz / Cadastro diz" com checkbox obrigatório antes de Aprovar/Disparar. Tela de envio de lista agora mostra o endereço que vai pra mensagem assim que uma agenda é selecionada, com aviso se não bate com o que o PDF trouxe.
- **Relatório automático por e-mail + contato de secretaria removidos de vez**: `contactName`/`contactPhone`/`contactEmail` tirados do schema `Municipality` (migration `20260815020000`, aplicada em produção — campos estavam vazios, sem dado real perdido). `POST /api/lists/:id/conclude` só marca `CONCLUIDA` agora; relatório continua disponível só por download manual ("Exportar").
- **Cron diário ativado (só lembrete D-1) + resumo do gestor removido**: decisão do usuário — o único envio automático que interessa é o lembrete de véspera pro paciente; não existe mais resumo diário por e-mail pro gestor (`modules/reports/daily-summary.service.ts` apagado, rota `/api/cron/daily-summary` removida). `lib/email.ts`/Resend ficaram sem nenhum uso depois disso (mantidos por enquanto, ninguém pediu pra tirar a dependência). `vercel.json` ganhou `crons: [{ path: "/api/cron/queue", schedule: "0 12 * * *" }]` — 9h de Brasília, 1x/dia, cabe no plano **Hobby** (não precisa de Pro). O endpoint virou `GET` (Vercel Cron só invoca por GET, nunca POST — antes como POST o cron nunca teria disparado de verdade nem se `vercel.json` tivesse `crons` configurado). Esse mesmo cron agora faz um `SELECT 1` no Supabase antes de tudo, como keep-alive contra a pausa por inatividade do free tier.
- **Cadastro de Unidade juntado ao de Município**: aba "Unidades" removida de Configurações; cada card de município na aba Municípios já mostra as unidades dele com "Nova unidade" ali dentro (sem select de município, sem trocar de aba). `GET /api/catalog/units` continua existindo, só perdeu a tela própria.

## Ambiente local (já configurado nesta máquina)

**Histórico rápido, pra não repetir o erro**: entre 2026-07-25 e 2026-07-27 o `.env` desta máquina apontou sem querer pro Supabase de produção (`ANTHROPIC_API_KEY`/`WHATSAPP_ACCESS_TOKEN` já eram reais também) — cada request cruzava a rede até São Paulo e a Revisão de lista chegou a levar 7-9s pra carregar. Corrigido em 2026-07-27: dev volta a usar Postgres local via Docker, rápido (as mesmas rotas caíram pra 15-120ms).

- **Banco ativo**: Postgres local em Docker. Subir com `docker compose up -d` (usa o `docker-compose.yml` da raiz); esperar ficar `healthy` com `docker inspect --format='{{.State.Health.Status}}' dgs-postgres`. Migrations com `npx prisma migrate deploy` (nunca `migrate dev` — ver "Convenções operacionais"). Primeiro usuário com `npm run seed -- "Nome" email@dgs.local`.
- **`.env.production` guarda a string de conexão real do Supabase** (e as demais chaves) pra quando o projeto for de fato publicado — não é carregado automaticamente por nada, é só referência. **Nunca copiar esses valores de volta pro `.env` ativo sem confirmar explicitamente com o usuário antes** — foi exatamente essa troca sem querer que causou o incidente de 2026-07-25/27 (rodei `npm run seed` achando que era local e criei usuário em produção).
- `ANTHROPIC_API_KEY` e `WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID` no `.env` ativo **continuam reais** (não dependem do banco) — extração e envio de WhatsApp funcionam de verdade mesmo com o banco local. **"Disparar confirmações" e a cadência do dia mandam WhatsApp de verdade** mesmo em dev — pra testar sem risco, usar o card "Enviar teste" em Configurações → WhatsApp (`POST /api/whatsapp/signup/test-send`), que nunca consulta paciente/agendamento no banco.
- `wesley@dgs.local` era o usuário de equipe documentado aqui antes — ele existe no banco de **produção** (Supabase), não no Postgres local novo. No banco local atual não há usuário até alguém rodar `npm run seed`.
- **Corrigido**: `npm run dev:api` força `PORT=3000` via `cross-env`, porque o harness de preview injeta `PORT=5173` (a porta do Vite) no ambiente e o `--env-file` do Node não sobrescreve variável já existente — sem o `cross-env` a API tentava subir na mesma porta do Vite e o login falhava com 502. Não reverter esse script.
- Pra rodar: `npm run dev` (sobe API na 3000 + Vite na 5173 com proxy `/api`), ou usar o preview do harness com o config `dgs-web` (aponta pra porta 5173). Trocar algo no `.env` exige reiniciar o processo da API (`tsx watch` só recarrega o `--env-file` quando o processo reinicia — tocar em `src/server.ts` força o restart sem precisar matar o terminal).
- Cadastro está **vazio** de propósito (nenhum município/médico/procedimento) — é o estado real de primeiro acesso local, não precisa popular a menos que peçam.

**Backend** (`src/`):
- `config/env`, `lib/` (prisma, auth scrypt, phone E.164, whatsapp, templates, timezone, csv, http, errorReporting)
- `middleware/` (sessão, auth, rate limit no Postgres, errorHandler, rawBody)
- `modules/auth` — login com regeneração de sessão
- `modules/audit` — trilha de toda alteração manual
- `modules/catalog` — municípios, unidades, médicos, procedimentos, procedimento×médico com valores
- `modules/agendas` — escala do médico (âncora das listas complementares)
- `modules/extraction` — parsers locais (SISREG/CELK, sem IA), schema, mapeador para rascunhos — ver "Extração de listas: local, sem IA"
- `modules/lists` — preview (sugere município/agenda antes de criar a lista), upload, extração assíncrona, edição na revisão, aprovação
- `modules/queue` — fila com throttle e teto diário da Meta
- `modules/whatsapp` — webhook (respostas, statuses de entrega, opt-out), idempotente por wamid
- `modules/closings` — checks 2 e 3 + `closings.alerts.ts` (módulo puro de inconsistências)
- `modules/indicators` — as 4 taxas + repasse/faturamento/margem, e export CSV. Núcleo puro em `indicators.ts` (testado, `indicators.test.ts`), `indicators.service.ts` só busca no banco e chama ele — mesmo padrão de `suggestions`/`closings`. **Repasse/faturamento/margem continuam calculados, mas a UI esconde os três** (🚧 "em desenvolvimento" em Indicadores e em Configurações → Procedimentos por médico) — fora de escopo desde 2026-08-09, decisão reforçada em 2026-08-15.
- `modules/team` — equipe (criar, redefinir senha, ativar/desativar, trocar a própria) e leitura da trilha de auditoria
- `modules/suggestions` — `suggestions.ts` (puro, testado) calcula overbooking pela taxa histórica; `suggestions.service.ts` busca o histórico em cascata (médico+procedimento → médico → município → global)
- `modules/queue/cadence.service` — lembrete D-1, reenvio pelo telefone alternativo e expurgo LGPD; acionado pelo cron diário (`GET /api/cron/queue`, 9h Brasília — ver `vercel.json`) ou na mão por `POST /api/queue/run-cadence`
- `modules/replies` — classificação por IA de resposta em texto livre ambígua (só entra quando `classifyReply()` puro devolve "unknown"); corte de confiança 0,7, abaixo disso fica "unknown" mesmo assim
- `modules/lists/list-report.ts` — geração do CSV do relatório de uma lista, reaproveitada pelo export manual (não tem mais e-mail automático nenhum)
- `lib/email.ts` — wrapper do Resend; sem uso nenhum no código desde que as duas features de e-mail (relatório à secretaria e resumo ao gestor) foram removidas — mantido só porque ninguém pediu pra tirar a dependência
- Reposição de vagas: `GET /api/agendas/:id/open-slots` (+ `/export`) lista os horários que abriram (recusa/sem resposta/sem telefone); lista complementar (`isComplementary`) dispara com o template `VAGA_ABERTA` em vez de `CONFIRMACAO`
- `modules/whatsapp/whatsapp-account.service.ts` — credenciais do WhatsApp: prioriza a conta conectada via Embedded Signup (tabela `WhatsappAccount`, sempre no máximo uma linha) e cai pro `.env` (`WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`) só como fallback de sandbox/dev. `signup.routes.ts` expõe a troca do `code` do Embedded Signup por token de longa duração — ver aba WhatsApp em Configurações
- `lib/whatsapp-templates.ts` (2026-08-18) — automação do que falta depois do Embedded Signup pra um número novo conseguir enviar: `registerPhoneNumber()` (registro na Cloud API, sem isso todo envio dá erro 133010) e `submitDefaultTemplates()` (submete os 3 templates padrão do produto — necessário porque cada WABA nova nasce sem nenhum template aprovado, erro 132001). As duas rodam sozinhas dentro de `POST /api/whatsapp/signup/callback`, best-effort. `getTemplateStatuses()` consulta ao vivo o status de aprovação na Meta — base do aviso "Templates aguardando aprovação" em Configurações → WhatsApp.
- `modules/conversations` (2026-08-18) — tela de conversas do WhatsApp: mostra e responde **qualquer** mensagem trocada com o paciente, não só a parte de confirmação de agendamento (que já tinha lugar próprio em Acompanhamento). Não criou tabela nova — `WhatsappMessage` já guardava cada mensagem enviada/recebida desde que a fila e o webhook existem, só faltava agrupar por telefone e existir um jeito de mandar texto livre (`sendText()` em `lib/whatsapp.ts` já existia, pronta, mas nunca tinha sido chamada por ninguém até agora). `listConversations()`/`getThread()` normalizam telefone em JS (formato varia entre envio e recebimento — mesmo problema de sempre, ver `phoneCandidates` em `lib/phone.ts`, que virou helper compartilhado nessa mesma leva). `sendReply()` bloqueia (409) fora da janela de 24h da Meta — só template reabre a conversa depois disso, mensagem livre não passa. Pra esse caso, `sendTemplateReply()` manda um dos 3 templates padrão direto da tela (campos definidos em `TEMPLATE_FIELDS`, `lib/templates.ts`) — funciona a qualquer momento, é a válvula de escape documentada quando a janela fechou.
  - **Dois bugs achados e corrigidos em 2026-08-24**: (1) `listConversations()` calculava a janela de 24h olhando só a mensagem mais recente (fosse nossa ou do paciente) — mandar uma mensagem por último "escondia" que o paciente tinha respondido há pouco e a janela ainda estava aberta. Corrigido rastreando a última mensagem **RECEBIDA** separado da mais recente em geral. (2) Mensagem de template nunca guardava o texto final, só o nome do modelo (`WhatsappMessage.body` ficava `null`) — a tela mostrava `[modelo: CONFIRMACAO]` em vez da mensagem de verdade. `renderTemplateText()` (`lib/whatsapp-templates.ts`) reconstrói o texto a partir do template + variáveis e agora é gravado em `body` nos três lugares que criam `WhatsappMessage` de template (`queue.service.ts`, `signup.routes.ts` test-send, `sendTemplateReply()`) — sucesso e falha, os dois. Registros antigos (antes desse fix) continuam mostrando só o nome do modelo, sem retroatividade.
  - **Achado no mesmo dia**: `POST /api/whatsapp/signup/test-send` não gravava `WhatsappMessage` nenhuma — por isso o aviso de billing (que olha o último envio registrado) ficava preso num teste antigo mesmo depois de corrigir e testar de novo com sucesso. Corrigido pra gravar sucesso e falha, igual o disparo de verdade já fazia.
  - **Terceiro bug achado no mesmo dia**: resposta por **clique de botão** (Sim/Não) aparecia em branco (`—`) em Conversas — o texto do clique vem em `buttonPayload`, não em `body`, e a tela só sabia mostrar `body`/`template`. Corrigido: `previewBody()` e a renderização da thread agora caem pra `buttonPayload` antes do `—`.
  - **Redesenho visual (2026-08-25)**: a tela crescia com o conteúdo e rolava a página inteira. Trocado por janela de altura fixa (`h-[75vh]`) com rolagem só dentro do painel de mensagens — mesmo padrão do `barbearia-saas`/`odonto-saas` (`webroot/chat.html` neles: `.phone` com altura fixa + `.messages` com `overflow-y: auto`). Visual também virou o do WhatsApp de verdade (cabeçalho verde-escuro, fundo bege, bolhas com "rabinho" — branca recebida, verde-clara enviada), com tokens `--wa-header`/`--wa-bg`/`--wa-bubble-out`/`--wa-bubble-in`/`--wa-input-bar` novos em `index.css` (light + dark, WhatsApp Web tema escuro pro segundo). **Exceção deliberada e documentada** à regra "cor forte só em status de paciente" — pedido explícito do usuário pra imitar a janela do WhatsApp de propósito, não decoração aleatória.
  - **Layout mobile (2026-08-25)**: lista e thread ficavam lado a lado sempre, mesmo em tela pequena — no celular isso cobria a tela inteira de forma ilegível. Agora, abaixo de `md` (mesmo ponto de quebra que o menu lateral já usa), só uma coluna aparece por vez — lista sozinha até selecionar uma conversa, aí vira a thread sozinha (cheia a tela) com um botão **"← Voltar"** no cabeçalho pra sair. De `md` pra cima continua lado a lado como antes.
- `modules/cancellations` (2026-08-22) — cancelamento de agenda inteira quando o médico não pode atender. Diferente de Listas: não tem upload, parte de uma `Agenda` já cadastrada (`GET /api/cancellations/preview?agendaId=`) — município/unidade/médico/data vêm sozinhos dela. Motivo é texto livre, escrito uma vez, valendo pra todo mundo notificado naquele disparo (`POST /api/cancellations` cria um `CancellationBatch`, muda `Appointment.status` pra `CANCELADO` **na hora do disparo** — não espera o envio ter sucesso, é decisão da equipe, não do WhatsApp — e enfileira `MessageJob`s com o template `CANCELAMENTO`). Exclui da notificação quem já recusou, já foi cancelado antes (idempotência) ou pediu opt-out — mas notifica quem já tinha confirmado, porque a agenda inteira caiu. `processQueue()` (`queue.service.ts`) ganhou um caso especial: pra esse template, nunca sobrescreve `Appointment.status` depois do envio (nem em sucesso nem em falha) — só grava a mensagem, porque o cancelamento já é fato consumado antes de tentar enviar. Tela em `/cancelamentos`: escolher agenda → ver quem recebe → motivo → `ConfirmModal` (é irreversível, manda mensagem real) → histórico de cancelamentos já feitos abaixo, com detalhe por paciente em `/cancelamentos/:id` (status de entrega de cada mensagem — é o "mostra tudo que foi enviado" que o usuário pediu). Template novo (`cancelamento_consulta`) também entrou em `DEFAULT_TEMPLATES` (`lib/whatsapp-templates.ts`), então WABA nova já submete ele sozinha também; **na WABA de teste atual foi submetido na mão** (script), status `PENDING`. Botão de resposta rápida único: "Ciente, obrigado(a)" — clique vira mensagem normal em Conversas, sem lógica especial (não é confirmação/recusa, `classifyReply()` não reconhece esse texto, cai como "unknown" e só fica registrado).
  - **Achado nessa leva**: `submitDefaultTemplates()` logava erro (`console.error`) toda vez que resubmetia um template que já existia — a mensagem genérica da Meta ("Invalid parameter") não diferenciava duplicata de erro de verdade; só o `error_subcode: 2388024` ("Já existe conteúdo nesse idioma") diferencia. Corrigido pra checar o subcode certo.
  - **Segunda origem pro cancelamento (2026-08-25)**: além de partir de uma `Agenda` cadastrada, agora também aceita uma `List` enviada na hora, sem agenda nenhuma vinculada — pro caso de a agenda nunca ter passado pela plataforma. Reaproveita 100% o upload/extração de Listas (`POST /api/lists` sem `agendaId`, mesmo motor de sempre) — só que em vez de seguir pro fluxo normal (Revisão → Aprovar → Disparar com `CONFIRMACAO`), a tela de Cancelamento usa o `listId` resultante direto como origem. `CancellationSource` (`cancellations.service.ts`) é `{ agendaId } | { listId }`; `CancellationBatch.agendaId` virou opcional (migration aditiva, `agendaId`/`listId` — exatamente um preenchido, validado na aplicação, não no banco). `describeSource()` reconstrói data/médico/município a partir do primeiro `Appointment` da lista quando não tem agenda (se a lista tiver mais de um médico misturado, só o primeiro aparece — não afeta quem recebe o aviso). Aba nova em `/cancelamentos`: "Agenda já cadastrada" vs "Nunca passou pela plataforma (enviar PDF)", essa segunda faz upload + espera a extração (polling em `GET /api/lists/:id`) antes de liberar a revisão de quem vai ser notificado.

**Frontend** (`web/src/`): login, shell, Listas, Revisão (tabela + arquivo lado a lado + sugestão de confirmações), Acompanhamento, Conversas (todo mensagem trocada com o paciente, ver+responder), **Cancelamento** (agenda inteira, quando o médico não pode atender), Fechamento, Indicadores, Configurações (7 abas, incluindo WhatsApp/Embedded Signup — agora com editar/ativar-desativar em Unidade/Médico/Procedimento/Procedimento×Médico e editar em Agenda), Equipe e auditoria, `StatusBand`, `ConfirmModal`, `FormModal`, `ui.tsx`.

**Nunca executado de verdade**: o envio real de WhatsApp pro paciente (falta confirmar entrega — usar o card "Enviar teste") e o fluxo novo de upload/preview/popup de agenda no navegador (ver "Estado atual"). A extração em si **já foi validada de verdade** contra 4 PDFs reais/exemplo — não depende mais de credencial nenhuma. Os testes cobrem a lógica pura (telefone, classificação de resposta, mapeamento da extração, alertas de fechamento, os dois parsers de extração, casamento de nome pro preview), não a integração de rede/banco.

**Fase 2 majoritariamente completa** conforme escopo do PLANO.md: lembrete D-1 (agora automático via cron diário), reenvio por telefone alternativo, sugestão de confirmações, expurgo LGPD, classificação por IA de texto livre ambíguo, reposição de vagas via lista complementar. Relatório automático por e-mail e resumo diário pro gestor foram removidos de escopo por decisão do usuário (2026-08-15) — deixaram de fazer parte do produto.

**Score de no-show por paciente: deliberadamente não implementado.** O dado não existe — o médico informa um total de atendidos por dia, não quem compareceu. Qualquer score seria aproximação apresentada como fato. A função pura `noShowScore` está pronta e testada em `modules/suggestions/suggestions.ts` para quando houver presença por paciente. Ver o comentário no fim de `suggestions.service.ts`.

## Convenções de código

- **Antes de qualquer commit**: `npm run typecheck` (server + web) e `npm test` limpos.
- **`git push` e deploy na Vercel (`vercel --prod`) sempre, sem perguntar** — decisão de 2026-08-16, substitui a pausa de 2026-07-26. Depois de qualquer commit com `typecheck`/testes limpos, sobe pro GitHub e faz `vercel --prod` direto, sem esperar confirmação. Ações destrutivas (excluir dado real, mexer em credencial) continuam exigindo confirmação — isso é só sobre deploy de código.
- Backend segue o padrão da barbearia-saas: módulos em `src/modules/<nome>/` com `.routes.ts` / `.service.ts` / `.repository.ts`, alias `@/*` → `src/*`, rotas lançam `AppError` e deixam o `errorHandler` responder.
- **Nunca `alert()`/`window.confirm()`** — sempre `ConfirmModal` (`web/src/components/ConfirmModal.tsx`), inclusive para aviso de um botão só (`hideCancel`).
- Todo `<select>` usa a classe `.field`, que já traz `color-scheme: light dark` + fundo sólido. Sem isso o dropdown nativo abre branco no dark mode.
- Datas "de hoje" sempre por `localDateString()` (`src/lib/timezone.ts`), **nunca** `new Date().toISOString().split("T")[0]` — em UTC o dia vira antes da hora no Brasil.
- Cores: só `ink`/`ink-muted`/`sheet`/`board` e afins. **Verde, amarelo, vermelho e cinza são exclusivos de status de paciente** — não usar para nada decorativo, senão a cor perde o significado.

## Convenções operacionais

- **Dado de saúde é dado sensível na LGPD.** Nome + procedimento + CNS não podem aparecer em log, no Sentry (já tem scrub) nem em URL. CID-10 nunca vai para a mensagem de WhatsApp.
- **Nunca disparar WhatsApp real para telefone de paciente em teste.** Sem `WHATSAPP_ACCESS_TOKEN` + `WHATSAPP_PHONE_NUMBER_ID` o envio já vira log no console — desenvolver assim. Para testar de verdade, usar número próprio.
- Migrations: quando o banco existir, seguir o padrão da barbearia-saas — **nunca** `prisma migrate dev` (a tabela `session` criada em runtime pelo `connect-pg-simple` gera "drift" e o Prisma oferece resetar o banco). Criar a pasta de migration à mão e aplicar com `prisma migrate deploy`.
- Nunca aceitar chave/token/senha colada no chat — orientar configuração direto no dashboard (Supabase/Vercel/Meta).
- **Minimização de dados diante de pedido de autoridade pública ou do titular (LGPD art. 18)**: nunca abrir acesso à base inteira. Usar `npx tsx --env-file=.env scripts/exportar-dados-paciente.ts --cns=... [--incluir-mensagens]` — traz só o essencial de UM paciente (nome, telefones, agendamentos); conteúdo de mensagem e nota interna da equipe são opt-in, não vêm por padrão. Confirmar legitimidade do pedido com o jurídico antes de repassar qualquer coisa.
- Manter este arquivo e o PLANO.md atualizados a cada mudança relevante.

## Extração de listas: local, sem IA (2026-08-06)

A extração **não chama mais nenhuma API de IA**. Decisão de 2026-08-05, implementada em 2026-08-06: só entra PDF nativo (SISREG ou CELK) — a prefeitura não manda mais foto — e a leitura é um parser determinístico local (`pdf-parse` pra texto + regex), de graça e instantâneo (~0,5s contra ~30-120s da IA antes).

- `src/modules/extraction/parsers/detect.ts` — reconhece o formato pela assinatura no cabeçalho/rodapé do PDF (`SISREG`, `CELK` ou `OUTRO`).
- `src/modules/extraction/parsers/celk.ts` — CELK exporta uma linha de texto por paciente, direto: regex simples resolve.
- `src/modules/extraction/parsers/sisreg.ts` — SISREG quebra cada célula em uma linha separada, sem alinhamento de coluna, e a fragmentação **varia dentro do mesmo arquivo** (nome de campo às vezes sozinho na linha, às vezes colado ao campo seguinte por tab — ex.: código de solicitação com o dia da semana). O parser não confia em posição de linha: junta tudo do registro numa string e vai consumindo por **padrão de campo**, na ordem: código de solicitação → dia da semana → data → hora → CNS (15 dígitos, tolera 1 espaço perdido no meio) → nome → nascimento (idem, tolera quebra no último dígito do ano) → idade → origem (`CIDADE - UF`) → telefone(s) (tolera quebra logo após o hífen) → unidade solicitante → vaga (`1ª VEZ`/`RETORNO`) → CID-10 (lido só pra saber onde o registro termina — **nunca entra no resultado**, LGPD). Testado e validado campo a campo contra os 4 PDFs reais/exemplo disponíveis (100% de recall: 14/14, 50/50, 30/30, 23/23 pacientes, batendo com a contagem declarada no rodapé de cada relatório).
- `src/modules/extraction/parsers/shared.ts` — telefone (formatado e cru) e datas, compartilhado pelos dois parsers.
- `extraction.service.ts` mantém a mesma assinatura de antes (`extractList(file, mimeType) -> { result, usage }`) — só troca o motor por dentro. `usage` fica zerado (não há tokens, mas o campo continua existindo pra não quebrar `scripts/extrair.ts`). `extractionConfigured` agora é sempre `true` (não depende de chave nenhuma).
- Upload só aceita `application/pdf` agora (`ACCEPTED_TYPES` em `lists.routes.ts`, `accept=".pdf"` em `web/src/pages/Listas.tsx`) — imagem foi removida do fluxo inteiro.
- Formato `OUTRO` (não reconhecido) não trava a lista: ela cai em revisão vazia, com aviso pedindo preenchimento manual — não há mais fallback pra IA reconhecer.
- `extraction.prompt.ts` foi apagado (só existia pro prompt da IA). `@anthropic-ai/sdk` continua no projeto — ainda é usado por `modules/replies` (classificação de resposta ambígua), feature separada que não foi tocada.
- `pdf-parse` (`^2.4.5`) é dependência real agora, não só de teste — usa a API `PDFParse` (não a função antiga `pdf(buffer)` da v1).

## Upload de lista sugere município/agenda sozinho (2026-08-06)

Como a extração agora é local e instantânea, dá pra ler o cabeçalho do PDF **antes** de criar a lista e pré-preencher o formulário de upload — não precisa mais escolher tudo na mão toda vez.

- `POST /api/lists/preview` (`src/modules/lists/lists.preview.ts`) — recebe o arquivo, roda `extractList` (sem persistir nada) e tenta casar o que leu com o cadastro: município (`exact`, só igual — nunca por conter, ver abaixo), unidade/médico/procedimento (fuzzy, por conter — nomes costumam vir encurtados), e uma agenda já existente pra município+médico+data+unidade. Só sugere quando o candidato é único; ambíguo ou sem match fica `null` e a equipe escolhe na mão, como antes.
- `src/lib/text-match.ts` — `exactNameMatch` (só igual) vs `namesMatch` (igual ou contém). **Município usa `exact` sempre** — nunca fuzzy: "Camboriú" bate por conter dentro de "Balneário Camboriú" (município vizinho e diferente em SC), e escolher errado manda o relatório final pro contato da prefeitura errada. Unidade/médico/procedimento usam fuzzy — é normal o mesmo lugar aparecer encurtado ("SAIS" vs "SAIS SERVICO DE ATENDIMENTO INTEGRAL A SAUDE"). Achado testando: sem essa separação, o teste automatizado pegou o próprio bug do Camboriú/Balneário Camboriú.
- `web/src/pages/Listas.tsx` — escolher o arquivo já chama o preview e pré-seleciona Município e, quando existir, a Agenda. Se município e médico foram reconhecidos mas não existe agenda pra essa data, abre um popup (`FormModal`) pré-preenchido pedindo pra completar (capacidade nunca vem do PDF) e confirmar — só depois a lista é criada de fato, com `POST /api/agendas` rodando por trás se for preciso. O upload deixou de disparar sozinho ao escolher o arquivo (como era antes) — agora tem um botão "Enviar lista" explícito, porque o passo de preview/confirmação precisa de um momento antes de subir pra valer.
- **Achado no caminho, corrigido**: a tela "Nova agenda" em Configurações nunca teve campo de Unidade — o backend aceita `unitId`, mas o formulário não mandava. Toda Agenda criada por ali ficava com `unitId = null`, o que quebra a mensagem de WhatsApp (sem unidade vinculada, sai só o nome do município — é o aviso que já existe na revisão). Corrigido: campo Unidade adicionado ao formulário e à listagem.
- **Popup de cadastro de município (2026-08-09)**: se o preview leu um nome de município que não bate com nada cadastrado, abre um `FormModal` pedindo pra confirmar/cadastrar (nome pré-preenchido em Title Case a partir do que veio em CAIXA ALTA no PDF — `toTitleCase()` local em `Listas.tsx`, nunca inventa acento que o PDF não tinha). Ao cadastrar (`POST /api/catalog/municipalities`), encadeia automaticamente pro popup de agenda se médico+data também foram reconhecidos e não existe agenda pra esse município ainda (o preview original não checou agenda pra um município que nem existia — a checagem é refeita no cliente contra as agendas já carregadas). Verificado de ponta a ponta no navegador em 2026-08-09.

## WhatsApp em produção (2026-08-14/15)

- **Número que estava ativo até 2026-08-16**: `+55 47 8865-6379`, WABA "innova agendamentos" (nome de exibição ainda errado de propósito — trocar exige revisão da Meta, não é urgente). Estava conectado via fallback do `.env`, não pela tela — **removido de propósito das env vars da Vercel em 2026-08-16** pra forçar o teste do botão "Conectar WhatsApp" (ver "Estado atual" no topo do arquivo: produção está sem nenhuma credencial agora). `.env` local desta máquina continua com os valores antigos intactos (só a Vercel foi mexida).
- **Investigação do botão "Conectar WhatsApp" (2026-08-16) — dois bugs reais encontrados e corrigidos, mas ainda SEM confirmação de teste completo**:
  1. **CSP/COOP bloqueando o popup**: o padrão do helmet pra `Cross-Origin-Opener-Policy` é `same-origin`, que isola nossa página do popup do `connect.facebook.net` — o navegador corta a relação entre as janelas, o SDK da Meta perde a capacidade de saber quando o popup termina, e o `FB.login()` dispara o callback na hora com "cancelado" mesmo com o popup ainda aberto. Corrigido com `crossOriginOpenerPolicy: { policy: "same-origin-allow-popups" }` em `app.ts`.
  2. **Evento de coexistência ignorado**: a Meta manda `FINISH_WHATSAPP_BUSINESS_APP_ONBOARDING` (não `FINISH`) ao concluir conexão de número que já tem WhatsApp Business App — o listener de `postMessage` só reconhecia `FINISH`. Corrigido em `Configuracoes.tsx` (`SIGNUP_FINISH_EVENTS`).
  - Depois dos dois fixes, ainda apareceu "Login cancelado" — **causa não confirmada**: pode ser numa terceira coisa ainda não achada, ou pode ter sido só o bloqueio de rate-limit da Meta (ver abaixo) atrapalhando o teste. Ficaram **logs de diagnóstico temporários** (`console.warn("[WHATSAPP SIGNUP] ...")`) em `Configuracoes.tsx` — no `onMessage` (loga toda mensagem recebida, antes até do filtro de origem) e no callback do `FB.login()` (loga a resposta completa + `window.FB` antes de chamar login). **Remover esses logs depois que o fluxo for confirmado funcionando** — foram deixados só pra próxima tentativa de teste real.
  - **Bloqueio da Meta**: número de teste usado (`47988656379`) bateu no limite "You have requested a verification code too many times" — rate-limit por número (não por conta/app), libera sozinho entre 30min e 24h.
  - **Confirmado funcionando em 2026-08-18** com um número novo/limpo (sem histórico de WABA): popup completo até o fim, evento `FINISH` recebido, `POST /api/whatsapp/signup/callback` salvou a conta, aparece em Configurações → WhatsApp. Os logs de diagnóstico temporários citados acima ainda não foram removidos — próxima limpeza pendente.
- **App da Meta ficou travado em modo "Unpublished" (Development) até 2026-08-18** — quando o Renan (fora da equipe interna, sem papel cadastrado no app) tentou conectar via "número que já usa WhatsApp Business App", o popup do Facebook mostrou "Aplicativo inativo / This app is not accessible at the moment". Não é suspensão nem violação (Security Checkup confirmou "No violations") — é o comportamento padrão da Meta: **app em Development só deixa logar quem está em App roles** (Admin/Developer/Tester); qualquer pessoa de fora (como um cliente real, secretaria ou médico) cai nessa tela. Achado no painel `developers.facebook.com/apps/1681012396346095/settings/basic` (menu "Publish" com badge "Unpublished"). **Corrigido em 2026-08-18**: app publicado ("Your app was successfully published — now available for the public to use"). Isso destrava o Embedded Signup pra qualquer pessoa de fora da equipe, não só pra quem está em App roles — condição necessária pro produto funcionar com clientes reais. Ainda falta confirmar um teste de ponta a ponta pós-publicação (o rate-limit de código acima pode continuar valendo se usarem o mesmo número de teste).
- **`WHATSAPP_APP_ID` correto é `1681012396346095`** (16 dígitos) — teve um erro de transcrição pra `16810012396346095` (17 dígitos, um "0" a mais no meio) que quebrou a configuração de webhook por um bom tempo até ser achado. Se `/api/whatsapp/signup/config` ou qualquer chamada de app-access-token (`{app_id}|{app_secret}`) der "Invalid application ID", checar esse valor primeiro.
- **Billing da WABA precisa estar configurado** — sem moeda/forma de pagamento cadastrada, todo envio falha com erro 131042 "Business eligibility payment issue" (aceita a chamada, devolve `wamid`, mas o webhook de status volta `failed`). Configura em `business.facebook.com/billing_hub/accounts/details/?business_id=981536728049005&asset_id=<waba_id>&wizard_name=CHANGE_COUNTRY_CURRENCY&account_type=whatsapp-business-account`.
- **Webhook**: `callback_url` = `https://sistema-dgs.vercel.app/api/whatsapp/webhook`, campo `messages` assinado. A tela de "Webhooks" nem sempre aparece no painel da Meta pra apps configurados como Tech Provider (via Login do Facebook para Empresas) — nesse caso, configura direto pela Graph API (`POST /{app_id}/subscriptions` com `access_token={app_id}|{app_secret}`, `object=whatsapp_business_account`, `callback_url`, `verify_token`, `fields=messages`) em vez de procurar um campo na UI que pode não existir.
- **Failover manual entre números** (2026-08-15): `WhatsappAccount` agora aceita mais de uma linha, com `active` decidindo qual credencial o sistema usa (`getActiveCredentials()` em `whatsapp-account.service.ts`). Configurações → WhatsApp lista todas as contas conectadas via Embedded Signup, com botão "Usar este número" (troca ativa), "Remover" (com `ConfirmModal`) e "editar" (apelido interno, campo `label`, separado do `businessName` que vem da Meta). **A lista de contas e o botão de conectar precisam aparecer mesmo quando `status.source === "env"`** (é o estado atual de produção) — já corrigido depois de um bug em que ficavam escondidos nesse caso.
- **Coexistência (número que já tem WhatsApp Business App instalado, ex.: o da colega da equipe)**: roda pelo **mesmo popup do Embedded Signup no navegador** (não precisa de link só no celular como se pensava antes — ver [onboarding-business-app-users](https://developers.facebook.com/documentation/business-messaging/whatsapp/embedded-signup/onboarding-business-app-users/)); a Meta manda um código pro WhatsApp Business App do celular, a pessoa confirma lá e cola o código de volta no popup. Configurações → WhatsApp tem dois botões: "Conectar WhatsApp (número novo/limpo)" e "Conectar número que já usa WhatsApp Business App" — os dois caem na mesma `WHATSAPP_SIGNUP_CONFIG_ID` hoje (não existe config de coexistência separada — **confirmado em 2026-08-18 que não precisa existir**: não há toggle de coexistência em nenhuma etapa do wizard de criação de Login Configuration na Meta, o comportamento depende só da assinatura de webhook abaixo).
  - **Causa real do erro "This phone number is already registered to a WhatsApp account" ao clicar em "Conectar número que já usa WhatsApp Business App" — resolvida em 2026-08-18**: faltava o app estar inscrito (subscribed) nos campos de webhook extras que coexistência usa. Sem isso, a Meta trata qualquer número já registrado como bloqueio ("migre ou desconecte primeiro"); com isso, a doc oficial confirma que "a tela de seleção de WABA é substituída por uma tela que oferece conectar a conta WhatsApp existente". Corrigido chamando a Graph API direto (`POST /{app_id}/subscriptions` com `access_token={app_id}|{app_secret}`, mesmos `callback_url`/`verify_token` de sempre, `fields=` com todos os campos que já existiam **mais** `history,smb_app_state_sync,smb_message_echoes`) — confirmado via `GET /{app_id}/subscriptions` que os 13 campos (10 antigos + 3 novos) ficaram assinados. Config nenhuma na Vercel, nenhum deploy — é 100% do lado da Meta.
  - **Pendência real que sobra, não bloqueante**: a doc de coexistência também pede sincronizar contatos e histórico de mensagens do WhatsApp Business App via `POST /{phone_number_id}/smb_app_data` (`sync_type=smb_app_state_sync` e depois `sync_type=history`) **dentro de 24h** depois que a pessoa conecta — isso não está implementado em `signup.routes.ts`. Não impede enviar confirmação nova pra paciente (é só sobre trazer o histórico de conversa antigo do celular pra dentro do sistema), mas fica faltando se algum dia quisermos mostrar esse histórico na tela.
- **Cadência dispara globalmente**: `POST /api/queue/run-cadence` e `POST /api/lists/:id/dispatch` chamam `processQueue()`, que processa **todo** `MessageJob` `PENDENTE` do sistema, não só os da lista que você está olhando. Descoberto em 2026-08-15 quando aprovar uma lista de teste disparou também 14 jobs de uma lista antiga esquecida desde 2026-07-27.

## Extração de PDF na Vercel: dois bugs sérios já resolvidos (2026-08-15)

A extração funcionava perfeita local (Windows) mas quebrava em produção (Vercel/Linux) — dois problemas distintos, mesma raiz (dependências nativas do `pdf-parse`/`pdfjs-dist` que o rastreador de arquivos da Vercel não inclui sozinho no bundle serverless):

1. **`ReferenceError: DOMMatrix is not defined`** — `pdfjs-dist` tem `const SCALE_MATRIX = new DOMMatrix();` incondicional no topo do módulo de canvas, só usado pra *renderizar* página (nunca fazemos isso, só lemos texto). Ele tenta carregar `@napi-rs/canvas` pra conseguir esse polyfill, e se o binário nativo da plataforma não estiver disponível, não cai num fallback — derruba o processo inteiro. **Resolvido** definindo um "boneco" de `DOMMatrix`/`ImageData`/`Path2D` em `globalThis` antes de importar `pdf-parse` (`extraction.service.ts`) — o `pdfjs-dist` só tenta carregar o canvas de verdade quando esses globals ainda não existem, então o boneco evita a importação nativa por completo.
2. **`Cannot find module '.../pdfjs-dist/legacy/build/pdf.worker.mjs'`** — depois do fix acima, apareceu esse outro: o worker do `pdfjs-dist` é carregado dinamicamente (fora do alcance do rastreador estático de arquivos da Vercel), então não ia sozinho no bundle da function. **Resolvido** incluindo o pacote inteiro via `includeFiles` no `vercel.json` (`"{dist,dist-web,node_modules/pdfjs-dist}/**"`).

**Se algo parecido aparecer de novo** (outro `Cannot find module` ou `ReferenceError` de uma API de browser dentro de `pdf-parse`/`pdfjs-dist`): é quase sempre essa mesma categoria de problema — dependência nativa ou asset carregado dinamicamente que o `@vercel/nft` não rastreia. Primeiro tentar rodar `vercel --prod --force` (ignora cache de build, que já mascarou isso uma vez) antes de assumir que é bug novo.

## Notas técnicas que não são óbvias

- **A extração de listas não usa mais IA** (ver seção acima) — histórico: chegou a rodar com `claude-opus-5`, depois `claude-haiku-4-5-20251001`, antes de virar parser local em 2026-08-06. `@anthropic-ai/sdk` continua no projeto só por causa de `modules/replies` (classificação de resposta ambígua) — regra central lá: campo ilegível vira `null`/`"unknown"` com confiança baixa, nunca um chute.
- **`react-router-dom` fica na versão mais recente (7.18.x) mesmo com um aviso do `npm audit`.** O aviso é GHSA-2w69-qvjg-hvjx (CSRF em **RSC mode**), que não se aplica: usamos SPA com `BrowserRouter`, sem React Server Components nem server actions. A "correção" que o npm sugere é descer para 7.11.0, que é afetada por **13 outros** avisos. Não descer de versão.
- `@theme inline` no `index.css` mapeia os tokens do Tailwind para variáveis CSS próprias, que trocam no seletor `.dark`. Por isso as utilidades (`bg-sheet`, `text-ink`…) acompanham o tema sozinhas.
- O build do Vite sai em `dist-web/` e o Express serve dali em produção (`WEB_DIST` em `src/app.ts`); em dev são dois processos (`npm run dev` sobe os dois, com proxy `/api` do Vite para a porta 3000).
- `api/index.js` importa de `dist/`, não do TypeScript fonte — os aliases `@/...` já foram reescritos pelo `tsc-alias` no build.
- O webhook precisa tratar `statuses` (entrega/falha), diferente da barbearia-saas que os ignora: telefone errado na lista da prefeitura é rotina e vira indicador de qualidade devolvido à secretaria.

## Pendências (a maioria resolvida em 2026-08-14/15 — ver "Estado atual" no topo pro que falta de verdade)

Tudo abaixo já foi feito. Fica só como histórico de decisão — pra pendências reais em aberto, ver "Pendências abertas" no topo do arquivo.

1. ~~Projeto Supabase dedicado de produção~~ — **feito e em uso** desde 2026-08-14 (não é mais só referência em `.env.production` — é o banco ativo).
2. ~~Número de WhatsApp Business + token, Embedded Signup, templates~~ — **feito**: WABA própria ("innova agendamentos"), billing configurado, templates aprovados, webhook ativo, validado com pessoas reais. Ver "WhatsApp em produção" acima pros detalhes/armadilhas.
3. ~~Chave da API Anthropic para extração~~ — não é mais pendência (extração local desde 2026-08-06).
4. ~~Chave do Resend~~ — decisão do usuário: manter sem Resend por enquanto. **Decisão de 2026-08-15: as duas features de e-mail (relatório à secretaria e resumo ao gestor) foram removidas do código de vez** — não é mais pendência, não tem mais nada esperando o Resend.
5. ~~PDFs reais pra calibrar extração~~ — feito, agora com 3 PDFs de 2 prefeituras confirmados contra produção (Camboriú/CELK, Pomerode/SISREG).
6. ~~`doctor_fee`/`city_rate`~~ — fora de escopo (módulo financeiro não entra nessa fase).
7. ~~Cron/Plano Pro da Vercel~~ — **resolvido em 2026-08-15 sem precisar de Pro**: `vercel.json` roda `/api/cron/queue` 1x/dia (cabe no Hobby), automatizando só o lembrete D-1 (e o resto da cadência que já andava junto: reenvio, fechamento de vencido, expurgo LGPD).
8. ~~Hospedagem~~ — Vercel, publicado, `DATABASE_URL` trocada pra produção, `git push` + `vercel --prod` já rodados.
